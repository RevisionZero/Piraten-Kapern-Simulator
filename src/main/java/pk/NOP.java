package pk;

public class NOP extends Card{

    public NOP(){
        super("nop");  //The nop card does not need any special attributes, only a name is sufficient to identify it as a nop card
    }
}
